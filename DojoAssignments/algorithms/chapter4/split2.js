function split_two(str){
    let arr = [];
    for(let j=0; j<str.length; j++){
        // console.log(str[j]);
        arr[j] = str[j];
    }
    return arr;
}
curr_str = 'podemos?';
console.log(split_two(curr_str));